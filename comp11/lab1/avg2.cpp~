//
// avg2.cpp -- compute average of three numbers
//             demonstrates:
//		  input, output
//		  variables, assignment
//		  operations: +, / with ints, %, / with floats
//		  comments
// modified by: Benjamin Tanen
//          on: 1/2/2014
//

#include <iostream>

using namespace std;

int main()
{
	double	a, b, c, d;	// creates three storage boxes for ints
	double	sum;		// sum goes here
	double	quotient;
	// int	remainder;
	double	num;

	num = 4.0;		// number of values to average

	cout << "Please enter four numbers: ";

	cin  >> a;
	cin  >> b;
	cin  >> c;
	cin  >> d;

	// do the processing part now
	
	sum = a + b + c + d;	// add and store

	quotient  = sum / num;
	// remainder = sum % num;

	// output the result

	cout << "The average is " << quotient << " " << endl;
	// cout << remainder << "/" << num << endl;
	// cout << "In decimal form: " << (sum / 4.0) << endl;
}
